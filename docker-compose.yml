services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Optional: local MongoDB replica set for development
  # Uncomment the section below to spin up a local MongoDB instance
  # mongo:
  #   image: mongo:7
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo-data:/data/db
  #   command: ["--replSet", "rs0"]

# volumes:
#   mongo-data:
